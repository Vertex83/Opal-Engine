# Low-level language examples

These files provide reference implementations for optional low-level optimization helpers in C, Zig, D, and assembly.
They are meant to be used as drop-in examples when you want a project-specific fast path or fallback optimizer.
